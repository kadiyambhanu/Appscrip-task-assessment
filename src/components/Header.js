import Navigation from './Layout/Navigation';

/**
 * Header Component
 * Wraps the Navigation component in a semantic header element
 */
export default function Header() {
  return (
    <header>
      <Navigation />
    </header>
  );
}

